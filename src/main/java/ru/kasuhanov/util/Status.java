package ru.kasuhanov.util;

public enum Status {
    selectUser, joinRoom, getRooms, addRoom, disconnect, messasge, getUsers, quit,
    MESSAGE, LOGIN_OK, LOGIN_NOK, ROOMS, ROOM_ADD_OK, ROOM_ADD_NOK, JOIN_OK, JOIN_NOK
}
